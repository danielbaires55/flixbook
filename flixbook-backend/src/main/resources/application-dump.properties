# Modalità DUMP: avvia il backend contro un DB già popolato da un dump SQL
# - Disabilita Flyway (nessuna migrazione applicata)
# - Non tocca lo schema (ddl-auto=none)

spring.flyway.enabled=false
spring.jpa.hibernate.ddl-auto=none

# Se serve, puoi overrideare le credenziali via env all'avvio
# SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/flixbookdb?serverTimezone=Europe/Rome
# SPRING_DATASOURCE_USERNAME=root
# SPRING_DATASOURCE_PASSWORD=root